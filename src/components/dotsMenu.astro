---
import DotsIcon from "@/icons/dotsIcon.astro"

interface Props {
    userID: String
}

const { userID } = Astro.props
---
<div data-userid={userID} class="relative">
    <button class="text-stone-400 cursor-pointer hover:bg-stone-100 hover:text-stone-600 rounded-full p-0.5">
        <DotsIcon />
    </button>
    <section class="bg-stone-800/80 p-2 float-right rounded-md text-white flex flex-col gap-0.5 w-fit absolute">
        <button class="font-bold cursor-pointer hover:bg-stone-400 p-0.5">Editar</button>
        <button class="font-bold cursor-pointer hover:bg-stone-400 p-0.5">Deshabilitar</button>
    </section>
</div>

<script>
    const container = document.querySelector("[data-userid = ]")

    if (container !==)
</script>
